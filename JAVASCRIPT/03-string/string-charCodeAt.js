
const sentence = "Mi marido se ha ido a trabajar";
const index = 5;
console.log(`the character index ${index},is ${sentence.charCodeAt(index)}`);


// Curiosidad.
const icon = `💕`;
console.log(`the character point ${index},is ${icon.codePointAt(1)}`);

